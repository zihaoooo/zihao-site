# _local/ is committed (backup) but stripped from the deploy in
# .github/workflows/pages.yml, so it never appears on the live site.

# Claude Code — not website content
.claude/
CLAUDE.md

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
